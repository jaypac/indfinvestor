app:
  db:
    vendor: h2

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/C:/Jubin/dev/data/db/h2/projects/indfinvestor
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate