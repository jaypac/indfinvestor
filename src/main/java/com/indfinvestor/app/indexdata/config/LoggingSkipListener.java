package com.indfinvestor.app.indexdata.config;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
//import org.springframework.batch.core.listener.SkipListenerSupport;

public class LoggingSkipListener
        //extends SkipListenerSupport<Object, Object>
{

/*    private static final Logger logger = LoggerFactory.getLogger(LoggingSkipListener.class);

    @Override
    public void onSkipInRead(Throwable t) {
        logger.error("Skipped due to exception in read", t);
    }

    @Override
    public void onSkipInProcess(Object item, Throwable t) {
        logger.error("Skipped item in process: " + item, t);
    }

    @Override
    public void onSkipInWrite(Object item, Throwable t) {
        logger.error("Skipped item in write: " + item, t);
    }*/
}

